# its a test for basic server written in perl

```bash
# to build 
docker compose up -d --build

# to rebuild 

docker compose down && docker compose rm -f &&  docker compose up -d --build

# to see the logs 
docker compose logs -f
```
